<!doctype html>
<html {% if section %} data-current="{{ section }}"{% endif %} lang="en">

	{% include "components/head.njk" %}

	<body class="{{section}}">

		<header>
        <a href="/"><img src="/static/img/zrh-guitar.svg" alt=""></a>
				<h1>
          {{ title }}
        </h1>
		</header>

		<main>
			{{ layoutContent | safe }}
      {% if section === 'songs' %}
        <div class="backlink">
          <a href="/">&laquo;back</a>
        </div>
      {% endif %}
		</main>

		{% include "components/footer.njk" %}

	</body>

</html>
